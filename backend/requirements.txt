fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
openai==1.51.0
# httpx>=0.28 removed the `proxies` kwarg that openai==1.51.0 (via langchain-openai==0.2.2)
# still passes internally, causing: "Client.__init__() got an unexpected keyword argument
# 'proxies'". Pinning httpx below 0.28 keeps that old codepath working without having to
# bump openai/langchain to newer major versions. See https://github.com/openai/openai-python/issues/1903
httpx==0.27.2
langchain==0.3.3
langchain-openai==0.2.2
langchain-community==0.3.2
gdown==5.2.0
google-genai
